
public class Ares extends ConverterTest {
	// Method to convert Ares to acres
		public static double aresToAcres(double userValue) {
			double ares2Acres;

			ares2Acres = (userValue * 0.024711);

			return ares2Acres;
			        
		}
					
	// Method to convert Ares to hectares
		public static double aresToHectares(double userValue) {
		double ares2Hectares;

		ares2Hectares = (userValue / 100);

		return ares2Hectares;
		        
		}
					
	// Method to convert Ares to inches squared
		public static double aresToInSqrd(double userValue) {
		double ares2InSqrd;

		ares2InSqrd = (userValue * 155000.310001);

		return ares2InSqrd;
		        
		}
			
	// Method to convert Ares to meters squared
		public static double aresToMeterSqrd(double userValue) {
		double ares2MeterSqrd;

		ares2MeterSqrd = (userValue * 100);

		return ares2MeterSqrd;
		        
		 }
			
	// Method to convert Ares to feet squared
		public static double aresToFeetSqrd(double userValue) {
		double ares2FeetSqrd;

		ares2FeetSqrd = (userValue * 1076.391042);

		return ares2FeetSqrd;
		        
		}
			
	// Method to convert Ares to CmSqrd
		public static double aresToCmSqrd(double userValue) {
		double ares2CmSqrd;

		ares2CmSqrd = (userValue * 1000000);

		return ares2CmSqrd;
					        
		}
}
